<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartyatra">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />


    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Smart Yatra"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.SmartYatra"
        android:supportsRtl="true">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDHh99nwvonCvMHjY7YcC9SdR-huRX0jnI" />

        <!-- Login activity as app entry point -->
        <activity android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>



        <!-- Registration activity -->
        <activity android:name=".RegistrationActivity" />

        <!-- MainActivity with bottom navigation -->
        <activity android:name=".MainActivity" />

        <!-- Add other activities here -->
    </application>


</manifest>
